# 相容性與驗證矩陣

下表只保留公開安全、對實作者有用的結論；`已驗證` 代表對應行為已有實測或程式契約支持，`部分驗證` 代表只證明了部分路徑，`待補強` 代表目前實作仍保留明確缺口。

| 類別 | 項目 | 狀態 | 說明 |
| --- | --- | --- | --- |
| API | `POST /v1/chat/completions` | 已驗證 | 主要相容端點，支援一般文字與 SSE streaming。 |
| API | `POST /v1/responses` | 部分驗證 | 保留相容層，但主要 consumer 驗證仍集中在 `/v1/chat/completions`。 |
| API | `POST /v1/messages` | 部分驗證 | 保留 Anthropic 形狀相容層。 |
| Streaming | SSE delta + terminal + `[DONE]` | 已驗證 | partial event 與 terminal completion 可映射到標準 SSE 結尾。 |
| 文字政策 | `128000` UTF-16 Web compatibility | 已驗證 | 適合作為預設產品政策，不應描述成 backend hard limit。 |
| 文字政策 | 一般 `/v1` recovery metadata | Production 已驗證 | #44 已完成 deterministic 與 Production live 驗證：`/v1/chat/completions`、`/v1/responses`、`/v1/messages` 在 128001 UTF-16 時維持 `text_policy_exceeded`，並回 `caller_text_utf16`、`limit=128000`、`received=128001` 與 reduction-and-retry metadata。 |
| 文字政策 | `/memory/v1` overflow recovery | Production 已驗證 | #43 已完成 deterministic 與 Production live 驗證：HTTP 400 `context_length_exceeded` + `input is too long`，同時保留真實 UTF-16 metadata。Hindsight Reflect 使用 `max_context_tokens=40000`、Reflect retry `0` 的 canary 後，臨時 bank retain / recall / reflect 皆成功並完成清理。 |
| Conversation | strict hash-prefix checkpoint reuse | 已驗證 | 建議只在 prefix 完全一致時重用 upstream conversation。 |
| Private mode | `disableMemory=1` per connection | 已驗證 | 每條新 ChatHub WebSocket 都應重套。 |
| Private mode | no ordinary chat history outcome | 已驗證 | `disableMemory=1` 對 no-history outcome 有直接實效。 |
| Memory boundary | 長期 memory 由 consumer 管理 | 已驗證 | Sidecar 只保留短期 transport continuation state。 |
| Files | 一般文件 grounding | 已驗證 | 文件先取得 Microsoft file identity，再經 ChatHub annotation 引用。 |
| Files | 3 個 active attachments | 已驗證 | 文件與圖片共用同一 turn 的 active quota。 |
| Files | OneDrive / SharePoint staging side effect | 已驗證 | no-history 不等於沒有 staging copy。 |
| Vision | 單圖 / 多圖理解 | 已驗證 | 圖片會真正進入模型處理。 |
| Vision | 圖片與文件分流 transport | 已驗證 | 不應把兩條上游路徑硬合成一套。 |
| Built-in tools | Bing Web Search 單獨使用 | 已驗證 | citations / grounding 可出現。 |
| Built-in tools | Bing + `Code Interpreter` 同 turn | 已驗證 | 兩者可共存。 |
| Caller tools | 單一 caller tool continuation | 已驗證 | 基本 `tool_calls` / `tool_call_id` 路徑可成立。 |
| Caller tools | native Bing + caller tool coexistence | 部分驗證 | 能否共存高度依賴 tool-routing prompt wording。 |
| Caller tools | 同一 assistant turn 多個 caller tools | Production 已驗證 | #42 已改為模型生成前固定 ceiling：只有所有可選工具都明確 read-only 才允許 >1，其他情況事前序列化成 1；不再事後降級或截斷。Production 以兩個未帶 `readOnlyHint` 的 Hermes tools 實測第一輪只產生 1 個 `read_file`，第二輪同 session/tool result 正常續接並 `stop`。 |
| Tool results | 大型 tool result 完整保留 | 待補強 | 目前實作仍有 result flatten / truncation 風險。 |
| Code Interpreter | Python execution | 已驗證 | 上游可實際執行程式並回傳結果。 |
| Code Interpreter | output-file metadata | 已驗證 | 上游會產生正式 output-file reference。 |
| Code Interpreter | 對外可下載產出檔案 | 已驗證 | Sidecar 會以已登入身分擷取上游檔案、存入本機私有儲存區，再回傳短期下載網址。 |
| MCP | modern handler | 已驗證 | 程式樹內有 modern MCP 實作。 |
| MCP | legacy SSE handler | 已驗證 | 程式樹內保留 legacy handler。 |
| MCP | 呼叫端直接互通 | 部分驗證 | Modern 與舊版 SSE 路由均已掛載；實際相容性仍取決於呼叫端支援的傳輸與初始化流程。 |
| Generic OpenAI consumer / legacy Hermes-like integration | `/v1/chat/completions` | 已驗證 | 文字、streaming、單一 tool、Vision 與 continuation 已有正向證據；正式 Hermes 整合另使用 `/hermes/v1/chat/completions`。 |
| Hermes 類 consumer | Files API direct exercise | 未主張 | 既有 consumer 驗證重點不在 direct file object path。 |
| Hermes | `GET /hermes/v1/models`、`POST /hermes/v1/chat/completions` | Production 已驗證 | 專用 route、獨立 `hermes` checkpoint namespace、VM `/hermes/v1` 設定、#41 overflow recovery 與 #42 tool ceiling/continuation 均有 Production 正向證據。M365 model-specific context canary 已為 `80000`、`proactive_prune_tokens=41000`；實際 oneshot tool turn 完成 2 次 API call，deterministic prune boundary 也驗證 40999/41000 行為。 |
| Hindsight | `GET /memory/v1/models`、`POST /memory/v1/chat/completions` | 部分驗證 | Hindsight PoC 已跑通 retain／recall／reflect；Memory chat 不建立 Sidecar continuation checkpoint、會開新 ChatHub conversation。最新 FIFO、429 cooldown 與 fact-preserving repair guard 仍需最終整合回歸。 |
| Compatibility traffic | 互動流量優先 / Memory admission control | 部分驗證 | `/v1/chat/completions` 與 `/hermes/v1/*` 會阻擋新 Memory 入場；Memory concurrency、FIFO queue、holdoff 與 429 cooldown 以本地 deterministic test 驗證，不以高併發實機強制觸發 429。 |

---

# Compatibility and Validation Matrix

`Verified` means the behavior has direct test evidence or an enforced code contract. `Partially verified` means only part of the path is proven. `Needs work` marks a known implementation gap.

| Category | Item | Status | Notes |
| --- | --- | --- | --- |
| API | `POST /v1/chat/completions` | Verified | Primary general compatibility endpoint with text and SSE streaming. |
| API | `POST /v1/responses` | Partially verified | Compatibility layer remains available, but primary consumer validation centers on `/v1/chat/completions`. |
| API | `POST /v1/messages` | Partially verified | Anthropic-shaped compatibility layer remains available. |
| Streaming | SSE delta + terminal + `[DONE]` | Verified | Partial events and terminal completion map to a standard SSE ending. |
| Text policy | `128000` UTF-16 web compatibility | Verified | Product policy, not a claimed backend hard limit. |
| Text policy | generic `/v1` recovery metadata | Production verified | #44 passed deterministic and Production live verification: `/v1/chat/completions`, `/v1/responses`, and `/v1/messages` keep `text_policy_exceeded` at 128001 UTF-16 while returning `caller_text_utf16`, `limit=128000`, `received=128001`, and reduction-and-retry metadata. |
| Text policy | `/memory/v1` overflow recovery | Production verified | #43 passed deterministic and Production live verification: HTTP 400 `context_length_exceeded` plus `input is too long`, with the real UTF-16 metadata preserved. After canarying Hindsight Reflect to `max_context_tokens=40000` and Reflect retry `0`, a temporary bank completed retain / recall / reflect successfully and was cleaned up. |
| Conversation | strict hash-prefix checkpoint reuse | Verified | Upstream conversations are reused only when the history prefix matches exactly. |
| Private mode | `disableMemory=1` per connection | Verified | Reapplied for each new ChatHub WebSocket. |
| Memory boundary | long-term memory owned by consumer | Verified | The sidecar keeps only short-lived transport continuation state. |
| Files | document grounding | Verified | Documents obtain Microsoft file identity before ChatHub annotation. |
| Files | 3 active attachments | Verified | Documents and images share the same per-turn active quota. |
| Vision | single/multiple image understanding | Verified | Images are actually processed by the model. |
| Built-in tools | Bing Web Search | Verified | Citations and grounding can be produced. |
| Built-in tools | Bing + Code Interpreter | Verified | They can coexist in one turn. |
| Caller tools | single caller-tool continuation | Verified | Basic `tool_calls` / `tool_call_id` flow works. |
| Caller tools | native Bing + caller tool | Partially verified | Coexistence depends strongly on routing-prompt wording. |
| Caller tools | multiple caller tools in one assistant turn | Production verified | #42 fixes the ceiling before generation: limits above 1 require every selectable tool to be explicitly read-only; all other catalogs are serialized to 1. There is no post-generation downgrade or partial truncation. A Production Hermes test with two tools lacking `readOnlyHint` emitted exactly one `read_file` call and then continued normally with the same session/tool result to `stop`. |
| Tool results | large result preservation | Needs work | Flattening/truncation risk remains. |
| Code Interpreter | Python execution and artifact metadata | Verified | Upstream code execution and output-file references are proven. |
| Code Interpreter | downloadable artifacts | Verified | Authenticated fetch → private local storage → short-lived capability URL. |
| MCP | modern and legacy handlers | Verified | Streamable HTTP and legacy SSE routes are mounted. |
| Hermes | `GET /hermes/v1/models`, `POST /hermes/v1/chat/completions` | Production verified | The dedicated route, isolated `hermes` checkpoint namespace, VM `/hermes/v1` configuration, #41 overflow recovery, and #42 tool-ceiling/continuation behavior all have Production evidence. The M365 model-specific context canary is `80000` with `proactive_prune_tokens=41000`; a real oneshot tool turn completed two API calls and the native prune boundary test confirmed the 40999/41000 trigger behavior. |
| Hindsight | `GET /memory/v1/models`, `POST /memory/v1/chat/completions` | Partially verified | The Hindsight PoC has exercised retain / recall / reflect. Memory chat does not create a sidecar continuation checkpoint and starts a fresh ChatHub conversation; the latest FIFO, 429 cooldown, and fact-preserving repair guards still require final integration regression. |
| Compatibility traffic | interactive priority / Memory admission control | Partially verified | `/v1/chat/completions` and `/hermes/v1/*` block new Memory admission; concurrency, FIFO queueing, holdoff, and 429 cooldown are verified locally and deterministically. Live Microsoft tests do not intentionally force throttling. |
